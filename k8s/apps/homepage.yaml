# 1. Namespace
apiVersion: v1
kind: Namespace
metadata:
  name: apps
---
# 2. RBAC (Permissions for Homepage to read RAM/CPU)
apiVersion: v1
kind: ServiceAccount
metadata:
  name: homepage
  namespace: apps
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: homepage-role
rules:
  - apiGroups: [""]
    resources: ["nodes", "pods", "namespaces"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["metrics.k8s.io"]
    resources: ["nodes", "pods"]
    verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: homepage-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: homepage-role
subjects:
  - kind: ServiceAccount
    name: homepage
    namespace: apps
---
# 3. The Configuration Files
apiVersion: v1
kind: ConfigMap
metadata:
  name: homepage-config
  namespace: apps
data:
  kubernetes.yaml: |
    yggdrasil:
      mode: cluster

  settings.yaml: |
    title: "Yggdrasil Command Center"
    background: "https://images.unsplash.com/photo-1550684848-fac1c5b4e853?q=80&w=2070&auto=format&fit=crop"
    theme: dark
    layout:
      Infrastructure:
        style: row
        columns: 4
  
  widgets.yaml: |
    - kubernetes:
        cluster:
          show: true
          cpu: true
          memory: true
          showLabel: true
          label: "Yggdrasil"
        nodes:
          show: true
          cpu: true
          memory: true
          showLabel: true
  
  services.yaml: |
    - Infrastructure:
        - ArgoCD:
            href: "https://argocd.shasame.fr"
            description: "GitOps Engine"
            icon: "argocd.png"
        - Longhorn:
            href: "https://longhorn.shasame.fr"
            description: "Distributed Storage"
            icon: "longhorn.png"
        - AdGuard:
            href: "https://adguard.shasame.fr"
            description: "Network AdBlocker"
            icon: "adguard-home.png"
        - LLDAP:
            href: "https://lldap.shasame.fr"
            description: "User Directory"
            icon: "ldap.png"
---
# 4. The Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: homepage
  namespace: apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: homepage
  template:
    metadata:
      labels:
        app: homepage
    spec:
      serviceAccountName: homepage  # Attaches the RBAC permissions
      containers:
        - name: homepage
          image: ghcr.io/gethomepage/homepage:latest
          ports:
            - containerPort: 3000
          
          # Security & Connectivity Fixes
          env:
            - name: HOMEPAGE_ALLOWED_HOSTS
              value: "home.shasame.fr"
            - name: NODE_TLS_REJECT_UNAUTHORIZED
              value: "0"  # Ignores internal K3s self-signed certs
          
          # Keeps Homepage from eating the worker node's RAM
          resources:
            requests:
              memory: "50Mi"
              cpu: "50m"
            limits:
              memory: "150Mi"
              cpu: "200m"
          
          # Injecting files individually so the folder remains writable for logs
          volumeMounts:
            - name: config
              mountPath: /app/config/settings.yaml
              subPath: settings.yaml
            - name: config
              mountPath: /app/config/widgets.yaml
              subPath: widgets.yaml
            - name: config
              mountPath: /app/config/services.yaml
              subPath: services.yaml
            - name: config
              mountPath: /app/config/kubernetes.yaml
              subPath: kubernetes.yaml
      volumes:
        - name: config
          configMap:
            name: homepage-config
---
# 5. Service & Traefik Ingress
apiVersion: v1
kind: Service
metadata:
  name: homepage
  namespace: apps
spec:
  ports:
    - port: 3000
      targetPort: 3000
  selector:
    app: homepage
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: homepage
  namespace: apps
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`home.shasame.fr`)
      kind: Rule
      services:
        - name: homepage
          port: 3000
      middlewares:
        - name: auth-forward
          namespace: auth
  tls:
    certResolver: default